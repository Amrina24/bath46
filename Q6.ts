let full_name2 = " Amrina Salaar "
const rstrip = ("\t" + full_name2);
const lstrip = ("\t" + full_name2)
 const strip = ("\t" + full_name2)
console.log(full_name2)
console.log("Whitespace Stripping:\n\t" + rstrip + lstrip + strip)
